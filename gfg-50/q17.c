// write a program in c print all natural numbers up to N without using a semi-colon 

#include <stdio.h>

#define N 10 

int main(int val) {
    if(val<=N && printf("%d ",val) && main(val+1)) {
    }
}

